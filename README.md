# Task Management Project

---

Start the Backend:

use IntelliJ IDEA to run the backend project. Make sure you have the necessary dependencies installed and configured.

---

Start the Frontend:

1. `pnpm install` to install the dependencies.
2. `pnpm run start:dev` to start the frontend development server.

---

